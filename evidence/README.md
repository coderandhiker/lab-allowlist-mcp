# Captured evidence

Run `../scripts/capture-evidence.sh baseline|enforced|live` from the repository root.

Each phase stores stable evidence in its matching directory and regenerates `../REPORT.md`. Transient Copilot state and timestamped raw runs remain under ignored directories.
